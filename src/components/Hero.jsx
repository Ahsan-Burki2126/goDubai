import React from "react";

const Hero = ({ bgClass, title, desc }) => {
  return (
    <section className="h-full lg:h-screen md:h-screen xl:h-screen">
      <h1 className="font-bold">Hello World</h1>
    </section>
  );
};

export default Hero;
